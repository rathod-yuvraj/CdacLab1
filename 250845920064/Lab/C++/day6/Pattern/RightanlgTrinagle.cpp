    */
    /*
    // Number Pyramid
    std::cout << "Number Pyramid (Enter number of rows): ";
    std::cin >> n;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= n - i; j++) std::cout << " ";
        for (int j = 1; j <= i; j++) std::cout << j;
        for (int j = i - 1; j >= 1; j--) std::cout << j;
        std::cout << std::endl;
    }
    */
    /*
    // Floyd's Triangle
    std::cout << "Floyd's Triangle (Enter number of rows): ";
    std::cin >> n;
    int num = 1;
    for (int i = 1; i <= n; i++) {
        for (int j = 1; j <= i; j++) {
            std::cout << num++ << " ";
        }
        std::cout << std::endl;
    }
    */
    return 0;
}